# 프로젝트 작업 지침

## 미션 구현 전에 읽을 문서

미션 정의·게임 판정·관련 API·미션 UI·테스트를 수정하기 전에 아래 문서와 최신 판정·검증 기록을 읽는다.

0. [원작 50개 재대조](docs/ORIGINAL-MISSION-AUDIT.ko.md): 공식 자료와 사본의 범위, 창작 제목 제거, v3에서 수정한 판정.
1. [미션 규칙 기준 문서](docs/MISSIONS.ko.md): 1~50번 목표 수, 정확한 토큰 배열, 교신 조건, 특수 규칙, 미확정 판정 및 구현 기록 형식.
2. [구현 상태 대조 및 조사 근거](docs/MISSION-IMPLEMENTATION-AUDIT.ko.md): 번호별 구현 공백, BGA 보완 판정, 출처의 확인 수준, 구현 순서와 검증 결과.

3. [추가 판정 기록](docs/MISSION-RULINGS.ko.md): 12·16·17·5·33·41번 판본 차이와 채택 정책.
4. [최신 구현·검증 결과](docs/MISSION-IMPLEMENTATION-RESULTS.ko.md): 1~50번 실제 적용 및 테스트 범위.

관련 화면·인터페이스를 수정할 때는 [캐릭터와 테이블](docs/CHARACTERS-AND-TABLE.ko.md), [API 명세](docs/API.ko.md), [프론트 인계](docs/FRONTEND-HANDOFF.ko.md)도 읽는다.

## 범위와 현재 상태

- 대상은 **The Crew: The Quest for Planet Nine**, 한국어 PC·모바일 웹, 3~5인 기본형이다. Deep Sea, 추가 미션, 2인 JARVIS, 3인 도전 변형의 조건을 기본형에 섞지 않는다.
- 2026-09-14 공유 엔진·로컬 데모·Node 실시간 서버는 1~50번을 지원한다. Supabase 트랜잭션 저장소·Edge 원격 배포는 후속 작업이다. Node 공개 서비스는 `crew.bsh00.com`의 기존 Coolify/Cloudflare Tunnel을 사용한다(`docs/DEPLOYMENT.ko.md`). 작업 시 `shared/missions.json`과 현재 엔진·테스트에서 실제 상태를 확인한다.
- 미션 설명 열람, 시작 번호 설정, 규칙 조사 완료와 실제 플레이 지원은 별개다. 랜덤 추첨 범위도 실제 활성화된 미션을 기준으로 보고한다.
- `src/game/engine.ts`를 로컬 데모와 `server/rooms.ts`가 공유한다. 한쪽 화면만 고쳐 미션이 구현되었다고 보고하지 않는다.
- 기존 변경 사항과 카드·캐릭터 결과물을 보존한다. 원작 분홍색은 검은색 타마마 카드(`black`), 로켓은 흰색 표현으로 대응한다.

## 규칙을 코드로 옮길 때

- `modifiers` 설명 문자열로 판정하지 않는다. 목표 수, 절대/상대/Ω 토큰, 교신 정책, 준비 단계, 종료 조건을 구조화한다.
- `playable`만 변경해서 미션을 열지 않는다. 해당 미션의 준비 절차, 서버 판정, 화면 표시, 저장·복원 및 테스트를 함께 완성한다.
- 목표 0개 미션은 일반 목표 선택 단계를 건너뛰고 별도 종료 조건을 사용한다. 빈 배열의 `every()`로 성공 처리하지 않는다.
- 절대 순서, 상대 순서, 마지막 목표(Ω), 마지막 트릭을 구분한다. 같은 트릭에서 획득한 목표는 집합으로 판정하며 카드 제출·애니메이션 순서에 의존하지 않는다.
- 정보 축소 교신(D)을 ‘유일한 카드’로 저장하거나 표시하지 않는다. 교신 시작 트릭과 지정 대원의 교신 금지도 서버에서 검사한다.
- 준비 질문·응답, 지정 역할, 토큰 편집, 카드 이동, 목표 양도는 재접속 가능한 상태로 저장한다. 재요청으로 이동·교환이 중복 적용되지 않게 한다.
- 미션 직접 시작·랜덤 선택은 앞선 미션의 실행 상태를 전제로 하지 않는다. 실패 시 재도전과 중도 복귀는 같은 미션을 유지한다.
- 5인 양도 대상 및 46번의 논리적 좌석·색상 대응은 기준 문서를 따른다. 다른 사람의 손패나 비공개 목표를 예정된 공개 시점 전에 노출하지 않는다.

## 자료와 미확정 판정

- 출판사 공식 자료, 직접 읽은 항해일지 사본, 커뮤니티 해석을 구분한다. 직접 확인하지 못한 게시물로 명시된 원문 조건을 덮어쓰지 않는다. 새 제목·종료 조건을 임의로 추가하거나 테스트 통과를 원작 일치의 증거로 사용하지 않는다. 규칙 변경 시 버전을 올리고 진행 중인 판의 판정을 보존한다.

- 확인된 조건은 문서를 재사용한다. 미확정 항목이나 출처 충돌이 있는 부분에 한해 추가 조사하고 근거 URL·판본·확인일을 기록한다.
- 12번의 이미 교신한 카드 이동, 5·33·41번의 담당자 선택 범위, 준비 절차 조합, 16·17번 등의 종료 정책은 최신 판정 기록을 먼저 확인한다. 근거 수준이 C/I인 정책을 공식 확정 규칙으로 바꿔 적지 않는다.
- 미확정 판정을 원작 규칙으로 단정하지 않는다. 추가 자료나 BGA 실제 관찰, 또는 사용자 지정 규칙으로 결정한 경우 그 근거와 적용 범위를 구분해 기록한다. 관련 없는 미션 작업은 계속 진행한다.
- 제3자 항해일지 사본을 공식 번역본이라고 보고하지 않는다. 원작 서사와 번역문을 통째로 복제하지 않고 기능 조건을 자체 문장으로 정리한다.
- 원본 PDF·렌더링은 `artifacts/research/mission-audit/`에 있으며 Git 제외 대상이다. 다른 환경에 없으면 기준 문서의 출처 링크를 이용한다.

## 검증과 문서 갱신

- 미션별 성공·실패·경계 조건, 3/4/5인, 관련 준비 단계의 재접속, 지정 번호 시작·랜덤·재시도를 확인한다. 공통 규칙 테스트 통과만으로 50개 전수 검증을 주장하지 않는다.
- 변경 범위에 맞게 기존 명령을 사용한다: `npm test`, `npm run typecheck`, `npm run test:e2e`, `npm run test:e2e:server`. 문서만 수정했다면 링크·표의 일관성과 `git diff --check`를 확인한다.
- 계약 변경 시 `npm run contracts`로 파생 계약을 갱신하고 관련 API 문서와 호환성을 점검한다.
- 구현한 미션은 기준 문서의 기록 형식에 따라 규칙 확인·미확정 판정 해소·엔진 검증·실시간 검증을 각각 기록한다. 대조표의 상태와 확인일, 실행 목록을 함께 갱신한다.
- 아키텍처·코드 관계 조사와 코드 수정 후 그래프 갱신은 상위 `AGENTS.md`의 graphify 지침을 따른다.

## 최신 모달 UI 정책

- 일반 draft 방식의 목표 미션은 조건과 목표 선택을 한 모달에서 진행한다. 모달을 열 때 본인 준비 명령을 자동 전송하되, 서버의 전원 준비/선택 순서를 건너뛰지 않는다. 별도 확인 버튼을 다시 추가하지 않는다. 특수 미션 질문 응답은 자동 제출하지 않는다.
- 종료 결과는 모달로 표시하며 재도전/다음 임무는 방장의 명시적 버튼 클릭으로만 실행한다. 모달 닫기와 재접속은 게임 결과를 변경하지 않는다. 상세 동작과 검증 기록은 `docs/CHARACTERS-AND-TABLE.ko.md` 최신 절을 따른다.

## 사용자 지정 웹 정책 v4

`crew-p9-50-4`는 목표 색상 분산과 트릭 중 교신을 허용하는 사용자 지정 변형이다. 원작 공식 규칙으로 설명하지 않는다. 기존 시도는 교신 타이밍을 보존하며 재도전부터 v4를 사용한다. 전원 동의 재시작은 투표 상태/인증/중복 요청과 자동 트릭 타이머 중지를 함께 유지한다. 관련 정의는 `docs/MISSIONS.ko.md` 최신 절과 API 문서를 따른다.

## 배포 전 로컬 검증 정책 (사용자 요청, 2026-09-14)

다음 배포부터는 로컬에서 Aside로 실제 사용 흐름을 조작·확인하고, 문제 수정 및 재검증을 완료한 뒤 배포한다. 단위 테스트나 Playwright만 통과했다고 Aside 확인을 생략하지 않는다. Aside 연결 문제가 있으면 먼저 복구를 시도하고, 해결되지 않으면 생략한 채 배포하지 말고 사용자에게 상황을 알린다. 배포 후에는 공개 주소에서 HTTPS/API/실시간 연결을 확인한다. 이 정책이 요청되기 전에 이미 시작된 `53fbc1e` 배포는 진행 중 작업으로 마무리한다.

준비/교신 UI 후속 변경 시 브리핑부터 같은 고정 높이를 유지한다. 본인 신호→카드 선택→확인 모달→교신 전송 흐름과 D 표식, 준비 모달 반복 재열기/native close/재접속을 보존한다. 작은 손패 하단 교신 버튼이나 별도 큰 미션 패널을 다시 추가하지 않는다. 상세는 `docs/CHARACTERS-AND-TABLE.ko.md` 최신 절에 있다.
